[gd_scene load_steps=6 format=2]

[ext_resource path="res://assets/Scrap.png" type="Texture" id=1]
[ext_resource path="res://scrapmetal/ScrapMetal.cs" type="Script" id=2]

[sub_resource type="Shader" id=2]
code = "shader_type canvas_item;
uniform sampler2D emission_texture;
uniform vec4 glow_color : hint_color;

void fragment() {
	vec4 current_color = texture(TEXTURE, UV);
	vec4 emission_color = texture(emission_texture, UV);
	
	if (emission_color.r > 0f) {
		COLOR = (emission_color + glow_color);
	} else {
		COLOR = current_color;
	}
}"

[sub_resource type="ShaderMaterial" id=3]
shader = SubResource( 2 )
shader_param/glow_color = Color( 0, 0.0392157, 0, 0.0392157 )
shader_param/emission_texture = ExtResource( 1 )

[sub_resource type="CircleShape2D" id=1]
radius = 9.04341

[node name="Scrap" type="Area2D"]
script = ExtResource( 2 )

[node name="Sprite" type="Sprite" parent="."]
material = SubResource( 3 )
scale = Vector2( 0.5, 0.5 )
texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource( 1 )
